import sty from 'styled-components';

export const StyledLabel = sty.label`
`;

export const StyledLabelText = sty.span`
  display: inline-block;
  color: #a0a0a0;
  letter-spacing: 0.6px;
  font-size: 12px;
  font-weight: 500;
  text-transform: uppercase;
  padding: 0 0 3px 3px;
  cursor: pointer;
`;
